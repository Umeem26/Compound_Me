targets:
  $default:
    builders:
      drift_dev:
        options:
          # ISO-8601 text keeps millisecond precision and sorts correctly as
          # long as every stored value is UTC (see toStoredUtc).
          store_date_time_values_as_text: true
